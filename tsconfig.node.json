{
   "compilerOptions": {
    "composite": true,          // Required for project references
    "module": "ESNext",         // Keep existing
    "moduleResolution": "bundler", // Keep existing or add
    "allowSyntheticDefaultImports": true, // Keep existing
    "noEmit": false             // Important: This must be false for composite builds
  },
  "include": ["vite.config.ts"]
}
